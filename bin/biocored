#!/usr/bin/env node

'use strict';

var path = require('path');
var biocored = require('biocore-node/lib/cli/bitcored');
var servicesPath = path.resolve(__dirname, '../');
var additionalServices = ['insight-bio-api', 'insight-bio-ui'];
biocored(servicesPath, additionalServices);
